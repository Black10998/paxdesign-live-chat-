import SwiftUI

enum PAXBrand {
    /// PAXDesign brand accent — lime green.
    static let accent = Color(red: 194 / 255, green: 1.0, blue: 0)
    static let launchBackground = Color(red: 0.06, green: 0.07, blue: 0.09)

    /// Minimum branded splash time before transitioning to the shell.
    static let launchDuration: TimeInterval = 0.35
}
