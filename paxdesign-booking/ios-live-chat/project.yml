name: PAXDesignLiveChat
options:
  bundleIdPrefix: at.paxdesign
  deploymentTarget:
    iOS: "16.0"
  xcodeVersion: "16.0"
  developmentLanguage: en
  knownRegions:
    - en
    - de
    - ar
settings:
  base:
    SWIFT_VERSION: "5.9"
    TARGETED_DEVICE_FAMILY: "1,2"
    MARKETING_VERSION: "1.57.0"
    CURRENT_PROJECT_VERSION: "96"
    DEVELOPMENT_TEAM: ""
    CODE_SIGN_STYLE: Automatic
targets:
  PAXDesignLiveChat:
    type: application
    platform: iOS
    sources:
      - path: PAXDesignLiveChat
    dependencies:
      - target: PAXWidgets
        embed: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: at.paxdesign.livechat
        TARGETED_DEVICE_FAMILY: "1,2"
        SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD: NO
        INFOPLIST_KEY_CFBundleDisplayName: PAXDesign Live Chat
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations: "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_NSUserNotificationsUsageDescription: "Notifications for live agent requests and new customer messages."
        INFOPLIST_KEY_NSFaceIDUsageDescription: "Face ID is used to unlock the app after inactivity."
        INFOPLIST_KEY_NSPhotoLibraryUsageDescription: "Select photos from your library to send in live chat."
        INFOPLIST_KEY_NSCameraUsageDescription: "Take photos to send in live chat conversations."
        INFOPLIST_KEY_NSLocationWhenInUseUsageDescription: "Location is used to verify device sign-ins and support secure staff device management."
        INFOPLIST_KEY_ITSAppUsesNonExemptEncryption: NO
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
        INFOPLIST_KEY_NSSupportsLiveActivities: YES
        ENABLE_PREVIEWS: YES
    entitlements:
      path: PAXDesignLiveChat/PAXDesignLiveChat.entitlements
    info:
      path: PAXDesignLiveChat/Info.plist
      properties:
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        CFBundleDisplayName: PAXDesign Live Chat
        NSCameraUsageDescription: "Take photos to send in live chat conversations."
        NSFaceIDUsageDescription: "Face ID is used to unlock the app after inactivity."
        NSLocationWhenInUseUsageDescription: "Location is used to verify device sign-ins and support secure staff device management."
        NSPhotoLibraryUsageDescription: "Select photos from your library to send in live chat."
        NSUserNotificationsUsageDescription: "Notifications for live agent requests and new customer messages."
        ITSAppUsesNonExemptEncryption: false
        CFBundleLocalizations:
          - en
          - de
          - ar
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UIBackgroundModes:
          - remote-notification
        CFBundleURLTypes:
          - CFBundleURLName: at.paxdesign.livechat
            CFBundleURLSchemes:
              - paxlivechat
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: false
        UILaunchScreen:
          UIColorName: LaunchBackground
  PAXWidgets:
    type: app-extension
    platform: iOS
    sources:
      - path: PAXWidgets
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: at.paxdesign.livechat.widgets
        TARGETED_DEVICE_FAMILY: "1,2"
        MARKETING_VERSION: $(MARKETING_VERSION)
        CURRENT_PROJECT_VERSION: $(CURRENT_PROJECT_VERSION)
        SKIP_INSTALL: YES
        LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/Frameworks @executable_path/../../Frameworks"
    entitlements:
      path: PAXWidgets/PAXWidgets.entitlements
    info:
      path: PAXWidgets/Info.plist
      properties:
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        CFBundleDisplayName: PAXDesign Widgets
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
  PAXDesignLiveChatTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: PAXDesignLiveChatTests
    dependencies:
      - target: PAXDesignLiveChat
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: at.paxdesign.livechat.tests
        GENERATE_INFOPLIST_FILE: YES
