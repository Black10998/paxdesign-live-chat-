name: Configure WordPress OpenAI Key

# Rotate the production WordPress option from GitHub secret only.
# Never accept a raw key via workflow_dispatch (those inputs are logged).
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - '.github/triggers/configure-wordpress-openai'

permissions:
  contents: read

jobs:
  configure-openai-key:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PAX_SITE: https://paxdesign.at
      PAX_APNS_TEAM_ID: "4ZSP8S5A7B"
      APNS_KEY_ID: ${{ secrets.APNS_KEY_ID }}
      APNS_TEAM_ID: ${{ secrets.APNS_TEAM_ID }}
      APNS_KEY_P8_BASE64: ${{ secrets.APNS_KEY_P8_BASE64 }}
      PAX_ADMIN_USER: ${{ secrets.PAX_ADMIN_USER }}
      PAX_ADMIN_APP_PASSWORD: ${{ secrets.PAX_ADMIN_APP_PASSWORD }}
      WP_SSH_HOST: ${{ secrets.WP_SSH_HOST }}
      WP_SSH_PORT: ${{ secrets.WP_SSH_PORT }}
      WP_SSH_USER: ${{ secrets.WP_SSH_USER }}
      WP_SSH_PRIVATE_KEY: ${{ secrets.WP_SSH_PRIVATE_KEY }}
      WP_PATH: ${{ secrets.WP_PATH }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: python3 -m pip install --quiet --break-system-packages PyJWT cryptography

      - name: Configure OpenAI on production via REST bootstrap
        env:
          SECRET_KEY: ${{ secrets.PAX_OPENAI_API_KEY }}
        run: |
          set -euo pipefail
          KEY="${SECRET_KEY:-}"
          if [ -z "$KEY" ]; then
            echo "Missing PAX_OPENAI_API_KEY GitHub secret" >&2
            exit 1
          fi
          echo "::add-mask::$KEY"
          export PAX_OPENAI_API_KEY="$KEY"
          unset KEY SECRET_KEY
          python3 scripts/configure-production-openai-remote.py

      - name: Configure OpenAI on production via SSH fallback
        if: failure()
        env:
          SECRET_KEY: ${{ secrets.PAX_OPENAI_API_KEY }}
        run: |
          set -euo pipefail
          KEY="${SECRET_KEY:-}"
          if [ -z "$KEY" ]; then
            echo "Missing PAX_OPENAI_API_KEY GitHub secret for SSH fallback" >&2
            exit 1
          fi
          echo "::add-mask::$KEY"
          export PAX_OPENAI_API_KEY="$KEY"
          unset KEY SECRET_KEY
          chmod +x scripts/configure-production-openai-key-ssh.sh
          ./scripts/configure-production-openai-key-ssh.sh
